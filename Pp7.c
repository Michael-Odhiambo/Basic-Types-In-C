//
// Created by michael on 07/01/2022.
//

/**
 * Modify programming project 6 from chapter 3 so that the user may add, subtract, multiply, or divide two fractions
 * ( by entering either +, -, *, or / between the fractions ).
 */

# include <stdio.h>

int main( void ) {

    int firstNumerator, firstDenominator, secondNumerator, secondDenominator;
    char operator;

    printf( "Enter two fractions together with the operator ( a/b + a/b ): " );

}

